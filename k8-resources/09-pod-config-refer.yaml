apiVersion: v1
kind: Pod
metadata:
  name: pod-config-refer-envar
  labels:
    name: roboshop
spec:
  containers:
  - name: nginx
    image: nginx
    envFrom:
    - configMapRef:
        name: pod-configmap