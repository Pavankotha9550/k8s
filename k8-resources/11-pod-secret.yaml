apiVersion: v1
kind: Pod
metadata:
  name: pod-secret
  labels:
    name: roboshop
spec:
  name: roboshop
  containers:
  - name: nginx
    image: nginx
    envFrom:
    - secretRef:
        name: pod-secret